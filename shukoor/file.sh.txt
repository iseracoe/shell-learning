#!/bin/sh
#author: shukoor

file="/home/shukoor/hi.sh"

if [ -r $file ]
then
        echo "file has read access"
else
        echo "file does not have read acces"
        fi

        if [ -x $file ]
        then
                echo "file has excute access"
        else
                echo "file does not have excute access"
        fi

        if [ -d $file ]
        then
                echo "it is a directory"
        else
        echo "it is not a directory"
        fi

then
                echo "file size is not zero"
        else
                echo "file size is zero"
        fi